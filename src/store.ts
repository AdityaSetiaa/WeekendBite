import {foods, regions, filterTags } from "./Foods/food.ts"
import weekendbites from "./assets/Weekend-bites.png"

const store = {
  foods,
  regions,
  filterTags,
  weekendbites
}

export default store